# Assets

`foot_template.png` is a fixed PNG template used by the pressure-map canvas.
It is derived from the left footprint in the user-provided double-foot design;
the canvas mirrors it for the right foot.

`foot_mask.png` is the alpha mask derived from the template image and is used
only to clip the transparent heatmap overlay inside the footprint shape.
