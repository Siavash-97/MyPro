# Secrets / API keys
*sk-proj*
*n8n key*
*.env
.env
*secret*
!*secret*.example
*credential*
*token*
*.pem

# Shortcuts and temp
*.lnk
python -m streamlit run*.txt
__pycache__/
*.pyc

# Logs and large data
FSR_LOG*.CSV
FSR_LOG*.csv

# Local test data
Data (Test)/

# Vercel CLI project link (machine-local, no secrets, just project/org ids)
.vercel/
